# Admin credentials (for local development)
# Generate password hash with: node -e "const bcrypt = require('bcryptjs'); bcrypt.hash('your-password', 10).then(h => console.log(h))"
ADMIN_USERNAME=admin
ADMIN_PASSWORD_HASH=$2b$10$MLZzZjQ57IqXxvOB2PPQzefbt6QxShFTEkKsOaTxIogartXw7ZhRW

# Session secret (generate with: openssl rand -hex 32)
SESSION_SECRET=change-me-to-a-random-string

# Site URL (used for SEO metadata)
NEXT_PUBLIC_SITE_URL=http://localhost:3000
